package main

import "fmt"

type field struct {
	name string
}

func (p *field) print() {
	fmt.Println(p.name)
}

func main() {
	str := []int{1, 2, 3}
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
	fmt.Println(len(str), cap(str))
}
